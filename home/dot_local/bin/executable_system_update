#!/usr/bin/env bash
set -euo pipefail

paru -Syu --batchinstall --sudoloop \
    --noconfirm --skipreview --failfast \
    --cleanafter --combinedupgrade

paru -Qtdq | paru -Rns - 2> /dev/null || printf "No packages to clean.\n"

flatpak update --assumeyes --system
